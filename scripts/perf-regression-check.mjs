#!/usr/bin/env node
// scripts/perf-regression-check.mjs
//
// Runs benchmarks for every rule under benchmarks/<rule>/bench.ts and
// compares each task's hz against benchmarks/<rule>/baseline.json.
// Fails if any task slows ≥10%, warns at 5–10%.
//
// Implementation: TODO. Generated by the bootstrap-ci skill as a stub
// so CI can wire it without a 404. The body needs to be written when
// the perf-regression-check skill is materialized.
//
// Tracked under the perf-regression-check Claude Code skill — see
// .claude/skills/perf-regression-check/SKILL.md for the contract.

console.log('[perf:check] TODO: implement perf regression check.');
console.log('[perf:check] Skipping — exit 0 so CI does not break.');
process.exit(0);
